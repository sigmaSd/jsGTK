{
  "name": "@sigmasd/gtk",
  "type": "module",
  "exports": {
    ".": "./src/gtk-ffi.ts"
  },
  "scripts": {
    "test": "deno install && deno fmt --check && deno lint && deno check && deno test --allow-ffi"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2"
  }
}
